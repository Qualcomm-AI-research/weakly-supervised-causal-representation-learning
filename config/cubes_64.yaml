defaults:
  - general: local
  - data: cubes_64
  - model: cubes_64_ilcm
  - training: cubes_64_ilcm
  - eval: default
  - plotting: default
  - hydra
  - _self_  # Ensures that entries in this file overwrite everything references above, even in hydra <= 1.0
