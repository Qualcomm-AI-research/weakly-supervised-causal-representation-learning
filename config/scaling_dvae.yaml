defaults:
  - general: local
  - data: scaling
  - model: scaling_dvae
  - training: scaling
  - eval: default
  - plotting: default
  - hydra
  - _self_  # Ensures that entries in this file overwrite everything references above, even in hydra <= 1.0
